<%= form_for([@post, @comment]) do |f| %>
  
  <% = render "shared/error_messages", :target => @comment %>

  <div class="field">
    <%= f.label :comment %><br />
    <%= f.text_area :comment, :size => "24x17" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
